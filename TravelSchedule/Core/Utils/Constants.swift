import Foundation

enum AppConstants {
    enum StationType {
        static let trainStation = "train_station"
    }
    
    enum TransportType {
        static let train = "train"
    }

    enum Locale {
        static let russian = "ru_RU"
        static let posix = "en_US_POSIX"
    }

    enum Country {
        static let russia = "Россия"
    }

    enum API {
        static let apiKeyKey = "YandexScheduleAPIKey"
    }
}

